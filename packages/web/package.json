{
  "name": "@counterexample-studio/web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --dir src --passWithNoTests"
  },
  "dependencies": {
    "@counterexample-studio/core": "file:../core",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
