# Isolated Heesch baseline verification

This research branch runs the unchanged public Yukon Heesch baseline only. It does not modify the main branch, submit a challenge entry, use paid runners, cache data, or upload billed artifacts. The official source revision and both baseline input hashes are checked before execution. All results remain in the GitHub Actions job log.
